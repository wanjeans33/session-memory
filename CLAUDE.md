# 全局规则与偏好（多端同步）

> 这是被 git 同步、跨 Mac / Windows / iPhone（云端会话）共享的全局记忆。
> 各机器的 `~/.claude/CLAUDE.md` 通过 `@import` 引用本文件。
> 在这里写**稳定**的规则/偏好；易变的事实写进 `memory/`。

## 关于我
<!-- 例：我是 …，主要用 …，时区 … -->

## 工作偏好
- 回答优先使用中文。

## 记忆索引
@./memory/MEMORY.md
